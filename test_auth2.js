const http = require('http');

const loginData = JSON.stringify({
    email: 'ram@gmail.com', // Let's guess Ram's email or something common, or we will grep the DB script. Let's just create a new driver and cancel their ride!
    password: '123'
});
